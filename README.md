## ansible-role-vault-dev

Runs Hashicorp Vault in dev mode.  Use on demo-devops as an ephemeral vault server to provide secrets to jcasc.  

### outputs
vault_root_token - root token of vault dev server
